# =============================================================================
# Lambda Backend – AWS Lambda Deployment Dependencies
# =============================================================================
# This file is a subset of requirements.txt optimised for Lambda package size.
# Heavy dev-only packages (mlflow[genai], litellm, …) are excluded.
# MLflow tracing code in the app gracefully degrades when mlflow is absent.
# =============================================================================

# Web framework
fastapi>=0.115.0

# Lambda adapter for FastAPI
mangum>=0.18.0

# Settings / validation
pydantic>=2.0
pydantic-settings>=2.0

# AWS SDK (Bedrock) - boto3 is available in Lambda runtime
boto3>=1.34.0

# HTTP client for Supabase REST queries
requests>=2.31.0

# PDF parsing for CV uploads
PyPDF2>=3.0.0

# Required by Starlette/FastAPI to parse multipart FormData (file uploads)
python-multipart>=0.0.9

# Environment variables
python-dotenv>=1.0.0

# PostgreSQL driver (used by CV storage / optional MLflow fallback)
psycopg2-binary>=2.9.0
